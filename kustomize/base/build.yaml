apiVersion: v1
kind: BuildConfig
metadata:
  name: web
spec:
  output:
    to:
      kind: DockerImage
      name: docker-registry.default.svc:5000/qualtrics-lti-dev/qualtrics-lti:latest
  runPolicy: Serial
  source:
    git:
      uri: https://github.com/tl-its-umich-edu/qualtrics-lti.git
      ref: "main"
    type: Git
  strategy:
    type: Docker
    dockerStrategy:
      dockerfilePath: Dockerfile.umich
      noCache: true
      forcePull: true
  triggers:
  - type: "GitHub"
    github:
      secretReference:
        name: github
  nodeSelector: {}
status:
  lastVersion: 0
